all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
    vpn_subnet: "10.8.0.0/24"

##examples how to create host inventory

  children:
    app-dev:
      hosts:
        app-development:
          ansible_host: 10.0.1.1
          public_ip: 198.51.100.10

    edge:
      hosts:
        app-edge:
          ansible_host: 10.0.1.2
          public_ip: 198.51.100.20
          vpn_server: true

    runner:
      hosts:
        github-runner:
          ansible_host: 10.0.1.3
          public_ip: 198.51.100.30

    vpn-servers:
      hosts:
        app-edge:
          ansible_host: 10.0.1.2
          public_ip: 198.51.100.20

    load-balancers:
      hosts:
        app-edge:
          ansible_host: 10.0.1.2
          public_ip: 198.51.100.20

    test-env:
      children:
        app-dev
        edge
